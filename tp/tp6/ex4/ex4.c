#include <stdio.h>

void readFile() {
    if(FILE *fp = fopen("clair.txt", "r")) {
        char buf[1024];
        while(fp = ) {

        }
    }
    fclose("clair.txt");
}

void writeFile() {
    //pass
}

int main() {
    printf("Hello World!\n");
    return 0;
}
